# GitHub fetch (this turn)

Cloned `https://github.com/zistgah/panini_by_grok` (depth 1) and
`https://github.com/project-ilm/legacy` (depth 1).

`diff -rq` against this workspace (excluding `.git`, `node_modules`, zip):
GitHub remote still lacks this session’s docs (dashboard, notebook Romenagri,
STATUS). Remote has MANIFEST.sha256 / OTS / attest not in the sandbox.
Do **not** push from here — zip is the handoff.

Original notebook retrieved:
`project-ilm/legacy/Notebooks/हिंदवी_2022_8_स्मरण_पुस्तिका.ipynb`
copied to `retrieved/legacy/Notebooks/`.
