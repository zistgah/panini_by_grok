# mez hosted inside panini_by_grok

Retrieved from `zistgah/mez`. Local-first desk (میز). Three rungs: console,
2D, immersive. This Pages copy is the **2D rung** so the Cognitive Workbench
is reachable on zistgah.org/panini_by_grok/ until spin-off.

- Full Python desk: remains `zistgah/mez` (`./mez serve` on 127.0.0.1:7373).
- Here: `docs/mez/index.html` (landing), `desk.html` (workbench).
- PANINI cyclers and `docs/nb.html` are tools the desk can seed; they are
  not mez internals.
