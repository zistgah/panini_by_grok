# PANINI-DROP:8f3c1a9e2b7d4c60

Unimportant files in this tree are emptied to that one line, or a directory holds PANINI-DROP.txt with that line.

Grep:

    grep -R "PANINI-DROP:8f3c1a9e2b7d4c60" .

Then delete those paths.

Marked this turn:
- public/site/standards/*-guru.html, *-shraeni.html, *-praatha.html (alias copies of -c/-cpp/-basic)
- artifacts/browsed_files, imagine_images, searched_images
- artifacts/panini/retrieved/standards (ISO/ECMA dumps)
- artifacts/panini/tests/iso (official-suite dumps)
- artifacts/panini/retrieved/ayebios (firmware blobs)
- tests/*.expected *.tags *.otags
