/* Copyright (C) 1993-2026 Abhishek Choudhary
 * SPDX-License-Identifier: GPL-3.0-or-later
 * Named-extract frontend. Workbench execution is host-speed (stdlower / appeval).
 * This module lexes and delegates to the host builtin — same slot as lua.pni.
 */
MODULE PANINI.Frontend.Scala

FUNCTION run_scala(source)
    n = LEN(source)
    lowered = SCALALOWER(source)
    r = run_c(lowered)
    r.frontend = "PANINI.Frontend.Scala"
    r.lowered = lowered
    r.bytes = n
    RETURN r
END

FUNCTION main()
    r = run_scala("object P {\n  def add(a: Int, b: Int): Int = a + b\n  def main(args: Array[String]): Int = {\n    val n = add(40, 2)\n    println(n)\n    return n\n  }\n}\n")
    RETURN r.value
END

END
