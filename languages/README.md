# Language Museum

Each specimen is a PANINI axis configuration that reproduces the *computational character* of a language family — not a full compiler of that language.

Status: architectural embedding (A), not a claim of source-compatible implementation.
