# PANINI.V2.WORKBENCH — L15 specification

Copyright (C) 1993-2026 Abhishek Choudhary  
SPDX-License-Identifier: GPL-3.0-or-later

Does **not** alter T0 (`spec/PANINI_SELF_HOSTING_SPEC.pni`). L15 is the tooling layer of the retrieved AGI taxonomy.

```
One Semantic Artifact → Many Human/Computational Projections
```

## Two environments (complementary)

1. **Desktop / VS Code** — LSP, debug, CI. Extension is a named GAP until skip=0 against LSP 3.17.
2. **Web workbench** — `docs/workbench.html` + `docs/nb.html` (frozen chrome) + `docs/console.html`. Zero-install. Not a toy; not a second VFS.

Progressive disclosure (beginner survives):

| Level | Surface |
|---|---|
| 0 | `PRINT "HELLO"` / Shaili BASIC / LOGO `forward 100` |
| 1 | `FUNCTION` |
| 2 | `@functional` |
| 3 | types, state, axes |
| 4 | AST / IR / proofs / memory |

Same language. Different visibility.

## Five identities of L15

1. IDE — Monaco (MIT) + vscode.dev. Not a reimplementation of VS Code.
2. Research laboratory — linguist lab, notebook, Sanskrit later (named).
3. Enterprise — architect uploader + misty OTS (outside this zip).
4. AGI laboratory — L0–L26 navigator (`docs/agi.html`), GENIE/FAKIR/cyclers.
5. Human-computing — ILM + Shaili/LOGO.

## Views of one artifact

Beginner · Engineer · Linguist · Mathematician · Compiler · Formal · AGI · System · Enterprise.

Changing the view does not create another program.

## Theorem

`T_WORKBENCH_L15`: specifying L15 does not rewrite T0, frozen hashes, or the Hindawi kernel.

Evidence: this file; `factory/agi_map.json`; `docs/data/agi-green.json` (derived).
